# Backend URL used by the production (Vercel) build. A real Vercel env var named
# VITE_API_BASE, if set, takes precedence over this file at build time — this is just a
# safe fallback so the deployed dashboard always points at the live Render API.
VITE_API_BASE=https://yash-1903-graphrag-api.hf.space
